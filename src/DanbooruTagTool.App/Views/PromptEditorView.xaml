<UserControl x:Class="DanbooruTagTool.App.Views.PromptEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources><BooleanToVisibilityConverter x:Key="BoolVisibilityLocal"/></UserControl.Resources>
    <Grid Margin="0,12,0,0">
        <Grid.ColumnDefinitions><ColumnDefinition x:Name="EditorColumn" Width="3*" MinWidth="350"/><ColumnDefinition Width="8"/><ColumnDefinition x:Name="EnglishColumn" Width="1*" MinWidth="225"/></Grid.ColumnDefinitions>
        <Border Grid.Column="0" Style="{DynamicResource PanelCard}" IsEnabled="{Binding CanEditPrompt}">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <StackPanel Grid.Row="0"><DockPanel LastChildFill="True">
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Count}" Style="{DynamicResource HintText}" VerticalAlignment="Center" Margin="0,0,10,0"/><TextBlock Text="表示:" Style="{DynamicResource HintText}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <ToggleButton Content="並び順" IsChecked="{Binding IsOrderedView, Mode=TwoWay}" Style="{DynamicResource ModeToggleButton}" Padding="8,4" MinHeight="30" FontSize="12" Margin="0,0,4,0" AutomationProperties.Name="並び順表示"/>
                        <ToggleButton Content="カテゴリ別" IsChecked="{Binding IsCategoryView, Mode=TwoWay}" Style="{DynamicResource ModeToggleButton}" Padding="8,4" MinHeight="30" FontSize="12" Margin="0" AutomationProperties.Name="カテゴリ別表示"/>
                        <CheckBox Content="英語も表示" IsChecked="{Binding CategoryEnglish, Mode=TwoWay}" VerticalAlignment="Center" Margin="8,0,2,0" Visibility="{Binding IsCategoryView, Converter={StaticResource BoolVisibilityLocal}}" AutomationProperties.Name="カテゴリ別英語表示"/>
                    </StackPanel>
                    <TextBlock Text="Prompt編集" Style="{DynamicResource PageTitle}" FontSize="22"/>
                </DockPanel><TextBlock Text="日本語表示のPromptを並べ替え・削除できます。見えている順序がそのままコピーされます。" Style="{DynamicResource PanelSubtitle}" Margin="0,3,0,10" Visibility="{Binding IsOrderedView, Converter={StaticResource BoolVisibilityLocal}}"/><TextBlock Text="Promptをカテゴリ別に読みやすく表示します。元の並び順・内容は変わりません。" Style="{DynamicResource PanelSubtitle}" Margin="0,3,0,10" Visibility="{Binding IsCategoryView, Converter={StaticResource BoolVisibilityLocal}}"/></StackPanel>
                <Border Grid.Row="1" Style="{DynamicResource ToolbarCard}" Padding="8" Margin="0,4,0,6" Visibility="{Binding IsOrderedView, Converter={StaticResource BoolVisibilityLocal}}"><DockPanel>
                    <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" IsEnabled="{Binding CanEditOrderedPrompt}"><ToggleButton Content="複数選択" IsChecked="{Binding MultiSelect}" Style="{DynamicResource ModeToggleButton}" Padding="8,5" MinHeight="32" FontSize="13" Margin="0,0,6,0"/><Button Content="選択を削除" Command="{Binding Delete}" Style="{DynamicResource CompactButton}"/><Button Content="Undo" Command="{Binding Undo}" Style="{DynamicResource CompactSecondaryButton}"/><Button Content="Redo" Command="{Binding Redo}" Style="{DynamicResource CompactSecondaryButton}"/></StackPanel>
                    <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center" IsEnabled="{Binding CanEditOrderedPrompt}"><TextBlock Text="Prompt内検索" Style="{DynamicResource HintText}" VerticalAlignment="Center" Margin="4,0,8,0"/><StackPanel Orientation="Horizontal" VerticalAlignment="Center" Visibility="{Binding HasFindQuery, Converter={StaticResource BoolVisibilityLocal}}" Margin="0,0,5,0"><TextBlock Text="{Binding FindMatchSummary}" Style="{DynamicResource HintText}" VerticalAlignment="Center" Margin="0,0,4,0"/><Button Content="↑" Command="{Binding FindPrevious}" Style="{DynamicResource CompactButton}" ToolTip="前の一致（Shift+Enter）" Padding="7,4" MinHeight="28" Margin="0,0,3,0"/><Button Content="↓" Command="{Binding FindNextCommand}" Style="{DynamicResource CompactButton}" ToolTip="次の一致（Enter）" Padding="7,4" MinHeight="28" Margin="0"/></StackPanel><TextBox x:Name="FindBox" MinWidth="80" Text="{Binding Find, UpdateSourceTrigger=PropertyChanged}" KeyDown="FindKeyDown" Padding="7,5" AutomationProperties.Name="Prompt内検索" ToolTip="Prompt内検索"/></StackPanel>
                </DockPanel></Border>
                <DockPanel Grid.Row="2" Margin="2,0,2,8" Visibility="{Binding IsOrderedView, Converter={StaticResource BoolVisibilityLocal}}"><TextBlock Text="{Binding SelectionSummary}" FontWeight="SemiBold"/></DockPanel>
                <Border Grid.Row="3" Style="{DynamicResource WorkSurfaceCard}" Padding="12" Visibility="{Binding IsOrderedView, Converter={StaticResource BoolVisibilityLocal}}"><Grid><ScrollViewer x:Name="EditorScroll" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" AllowDrop="True" DragOver="EditorDragOver" Drop="EditorDrop" DragLeave="EditorDragLeave" Margin="0,0,0,26"><ItemsControl x:Name="EditorItems" ItemsSource="{Binding Chips}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><Border x:Name="EditorChip" Style="{DynamicResource ChipCard}" MaxWidth="{Binding ActualWidth, ElementName=EditorScroll}" PreviewMouseLeftButtonDown="ChipMouseDown" PreviewMouseLeftButtonUp="ChipMouseUp" PreviewMouseMove="ChipMouseMove"><DockPanel><TextBlock Text="⋮⋮" Foreground="#7B93AA" FontWeight="Bold" Margin="0,0,8,0" VerticalAlignment="Top"/><Button DockPanel.Dock="Right" Content="×" Command="{Binding DataContext.DeleteOne, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Padding="2" Margin="8,0,0,0" MinHeight="22" Width="24"><Button.Style><Style TargetType="Button" ><Setter Property="Visibility" Value="Hidden"/><Setter Property="Background" Value="Transparent"/><Setter Property="BorderBrush" Value="Transparent"/><Setter Property="Foreground" Value="{DynamicResource MutedInkBrush}"/><Style.Triggers><DataTrigger Binding="{Binding IsMouseOver, ElementName=EditorChip}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></Button.Style></Button><TextBlock Text="{Binding Item.Display}" TextWrapping="Wrap" VerticalAlignment="Center"/></DockPanel></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></ScrollViewer><TextBlock Text="タグをドラッグして順序を変更できます。少ないPromptでもここが作業面です。" Style="{DynamicResource HintText}" VerticalAlignment="Bottom" Margin="6,0,6,2" IsHitTestVisible="False"/><Canvas IsHitTestVisible="False" ClipToBounds="True"><Border x:Name="InsertionMarker" Background="{DynamicResource PrimaryBrush}" Width="4" Height="38" Visibility="Collapsed" CornerRadius="2"/></Canvas></Grid></Border>
                <Border Grid.Row="3" Style="{DynamicResource WorkSurfaceCard}" Padding="12" Visibility="{Binding IsCategoryView, Converter={StaticResource BoolVisibilityLocal}}"><ScrollViewer x:Name="CategoryScroll" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><ItemsControl ItemsSource="{Binding CategoryGroups}"><ItemsControl.ItemTemplate><DataTemplate><Expander IsExpanded="True" Margin="0,0,0,4"><Expander.Header><StackPanel Orientation="Horizontal"><TextBlock Text="{Binding Label}" FontWeight="SemiBold" FontSize="13"/><TextBlock Text="{Binding CountText}" Style="{DynamicResource HintText}" FontSize="12" Margin="8,0,0,0"/></StackPanel></Expander.Header><ItemsControl ItemsSource="{Binding Items}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><Border Style="{DynamicResource CategoryChipCard}" ToolTip="{Binding English}"><StackPanel><TextBlock Text="{Binding Japanese}" TextWrapping="Wrap" FontSize="12"/><TextBlock Text="{Binding English}" TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource MutedInkBrush}" Visibility="{Binding DataContext.CategoryEnglish, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolVisibilityLocal}}"/></StackPanel></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></Expander></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></ScrollViewer></Border>
                <Border Grid.Row="4" Style="{DynamicResource AuxiliaryCard}" Visibility="{Binding WeightVisible, Converter={StaticResource BoolVisibilityLocal}}"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="110"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="選択タグのweight" Style="{DynamicResource HintText}" VerticalAlignment="Center" Margin="0,0,12,0"/><TextBox Grid.Column="1" Text="{Binding Weight}"/><Button Grid.Column="2" Content="適用" Command="{Binding ApplyWeight}" Style="{DynamicResource SecondaryButton}" Margin="10,0,0,0"/></Grid></Border>
            </Grid>
        </Border>
        <GridSplitter Grid.Column="1" DragCompleted="SplitterChanged"/>
        <Border Grid.Column="2" Style="{DynamicResource PanelCard}"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><StackPanel Grid.Row="0"><TextBlock Text="実際のEnglish Prompt" Style="{DynamicResource PageTitle}" FontSize="21"/><TextBlock Text="コピーされる内容をここで確認できます。" Style="{DynamicResource PanelSubtitle}" Margin="0,3,0,12"/></StackPanel><Border Grid.Row="1" Style="{DynamicResource PreviewCard}" Margin="0,0,0,14"><Grid><TextBox x:Name="EnglishPreview" Text="{Binding English, Mode=OneWay}" IsReadOnly="True" AutomationProperties.Name="実際のEnglish Prompt"><TextBox.Style><Style TargetType="TextBox" ><Setter Property="Visibility" Value="Visible"/><Setter Property="Background" Value="Transparent"/><Setter Property="BorderBrush" Value="Transparent"/><Style.Triggers><DataTrigger Binding="{Binding DirectEditing}" Value="True"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></TextBox.Style></TextBox><TextBox x:Name="DirectEditor" Text="{Binding DirectText, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" AutomationProperties.Name="英語Prompt直接編集"><TextBox.Style><Style TargetType="TextBox" ><Setter Property="Visibility" Value="Collapsed"/><Setter Property="Background" Value="#FFFDF2"/><Setter Property="BorderBrush" Value="#E3C98A"/><Setter Property="BorderThickness" Value="1"/><Style.Triggers><DataTrigger Binding="{Binding DirectEditing}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBox.Style></TextBox></Grid></Border><StackPanel Grid.Row="2"><Button Content="英語Promptをコピー" Command="{Binding Copy}"><Button.Style><Style TargetType="Button" ><Style.Triggers><DataTrigger Binding="{Binding DirectEditing}" Value="True"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></Button.Style></Button><Button Content="英語Promptを直接編集" Command="{Binding StartDirect}"><Button.Style><Style TargetType="Button" ><Style.Triggers><DataTrigger Binding="{Binding DirectEditing}" Value="True"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></Button.Style></Button><StackPanel Visibility="{Binding DirectEditing, Converter={StaticResource BoolVisibilityLocal}}"><TextBlock Text="直接編集モード" FontWeight="SemiBold" Foreground="#8A6A37" Margin="0,0,0,3"/><TextBlock Text="適用またはキャンセルまで、他のPrompt操作は一時停止します。未対応構文はrawのまま保持されます。" Style="{DynamicResource HintText}" Margin="0,0,0,8"/><WrapPanel><Button Content="直接編集を適用" Command="{Binding ApplyDirect}" Style="{DynamicResource PrimaryButton}"/><Button Content="キャンセル" Command="{Binding CancelDirect}" Style="{DynamicResource GhostButton}"/></WrapPanel></StackPanel></StackPanel></Grid></Border>
    </Grid>
</UserControl>
