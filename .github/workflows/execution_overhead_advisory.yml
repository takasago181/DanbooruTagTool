name: Execution Overhead Advisory

on:
  pull_request:
    paths:
      - "docs/project/**"
      - ".github/workflows/**"
      - "scripts/maintenance/audit_execution_overhead.py"
      - "scripts/maintenance/extract_compact_csv_slice.py"
      - "scripts/maintenance/test_extract_compact_csv_slice.py"
      - "docs/project/EXECUTION_TELEMETRY.md"
      - "scripts/maintenance/validate_execution_telemetry.py"
      - "scripts/maintenance/test_validate_execution_telemetry.py"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  advisory:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Test compact CSV transport
        run: python -m unittest scripts.maintenance.test_extract_compact_csv_slice

      - name: Test execution telemetry validator
        run: python -m unittest scripts.maintenance.test_validate_execution_telemetry

      - name: Run advisory execution-overhead audit
        run: python scripts/maintenance/audit_execution_overhead.py
