# V1 UI / INTERACTION DESIGN DISCUSSION — DRAFT

最終更新: 2026-09-13

> **DRAFT / NOT FROZEN / 変更前提**
>
> この文書は、Issue #66 の実装仕様を確定する前の設計議論メモである。
> `docs/PRODUCT_GOAL_LOCK.md` の高レベルな製品目的を置き換えない。
> 下記のUI構成・機能優先度・操作方法は、実利用を想像しながら今後変更してよい。
> この文書だけを根拠に大規模実装・旧実装削除・PR #67 のmergeを進めないこと。

## 1. 現時点でかなり強い方向性

DanbooruTagTool は「自動でPromptを完成させるAI」よりも、
**Danbooru系タグを日本語で理解・発見し、自分で選び、最終的に英語Promptとして扱えるローカル辞書兼作業ツール**を目指す。

現在の中心体験候補:

`見る / 読む -> 掘る / 探す -> 選ぶ -> Promptを整理する -> 英語でコピー`

特に、検索語を知らない初心者が「どんなタグがあるのか」を辞書のように眺め、ジャンルや関連概念から掘っていけることを重視する。

## 2. 辞書・検索側の暫定方針

### 辞書を検索しなくても一覧で見られる

- 日本語検索だけに依存しない。
- 辞書をそのまま開き、ジャンル別の一覧を眺められる。
- 一覧からそのままPromptへ追加できる。
- 日本語 / 英語 / 混合検索は同じ辞書探索に重なる補助手段とする。

### Special のジャンル分類を主な探索資産として活用する

完成済み #56 taxonomy を「棚」として使う。

例:

`ジャンル -> サブジャンル -> 該当Special一覧`

さらに、個々の概念から「関連・派生」を掘る体験を重視する。

例:

`ある概念 -> 関連行為 / 組み合わせ / 道具 / 状態 / 構図など`

重要:
- #56のジャンル階層と「関連・派生」グラフは別物として扱う。
- 関連・派生の根拠（manual relation / semantic / co-occurrence / lexical等）は未確定。今後設計する。
- 関連候補を勝手にPromptへ追加しない。

### General 30,629

- #64 が分類を継続する。
- #66 は #64 の分類を独自に作り直さない。
- #64完了後、Generalも辞書一覧・ジャンル探索から使えるようにする。
- #64未完了でもアプリの基本操作は成立する設計にする。

## 3. 表示は Japanese-first、出力は English

### 辞書表示

通常一覧はできるだけ静かにする。

基本表示候補:
- 日本語名
- canonical English tag
- 選択 / 追加操作

常時表示しない方向:
- `Special` / `General` の大きなbadge
- `登録済み` / `未登録` badge
- 重複警告badge
- 内部IDや監査状態などの開発情報

内部では必要なメタデータを保持してよいが、通常UIへ全部露出しない。

### 関連タグ

- 関連候補の見た目は日本語優先。
- canonical English は詳細表示や確認時に追跡できるようにする。
- 日本語と英語を常時すべて二重表示して画面を騒がしくしない。

### Prompt貼り付け時

既存Promptを貼ると、認識できたタグは日本語と英語を対応付けて理解できるようにする。

例:

- `青い髪` / `blue_hair`
- `こちらを見る` / `looking_at_viewer`

辞書にない要素:
- 消さない
- 勝手に推測置換しない
- raw textとして保持する
- 日本語訳がない場合は英語/raw表面だけでもよい

### コピー

UI上で日本語表示していても、通常の最終コピーは **英語Promptのみ**。
日本語は理解・確認のための表示層とする。

## 4. 辞書画面とPrompt編集画面は分離する方向

この設計が現時点の第一候補。

### A. 辞書 / 検索画面

主目的:
- タグを眺める
- 検索する
- ジャンルから発見する
- 詳細・関連・派生を見る
- Promptへ追加する

Promptについては、**現在のPromptをコンパクトに確認するだけ**に寄せる。

候補:
- 現在の選択数
- 日本語の簡易プレビュー
- 必要なら表示切替（日本語 / 日本語+英語 / 英語）
- `Promptを開く`
- `コピー`

この画面では本格的な並べ替え・大量の行操作をしない。

### B. Prompt編集画面

ワンクリックで切り替える専用作業画面。

主目的:
- 既存Promptの貼り付け / 読み込み
- 日本語付きで内容を理解する
- 順序変更
- 削除
- 最終英語Promptの確認 / コピー

検討中の機能:
- ON/OFF（一時無効化）
- 複数選択 / 一括操作
- weight編集
- 元Promptとの差分

上記はまだ採用確定ではない。

### 追加の入口

基本的に新しいタグの追加は:
- 検索結果
- 辞書一覧
- ジャンル閲覧
- 関連 / 派生候補

から行う。

Prompt編集画面を「第二の巨大辞書」にしない。

## 5. 実Prompt長を前提にする

短いUI mockupの5〜8タグだけを基準にしない。

Noob / WAI系の通常運用では、外見・衣装・ポーズ・構図・背景・style等を含めると数十タグになることを前提にする。

UI設計上は少なくとも:
- 30〜60タグ程度を普通に扱える
- 50〜80タグでも破綻しない
- 長いPromptで日本語+英語の2段表示が縦方向を圧迫しすぎない

ことを意識する。

そのため、辞書画面に常時フル編集リストを置くより、専用Prompt編集画面へ分離する案を優先している。

## 6. 画像スペースは置かない

現時点の通常アプリは画像生成・画像プレビュー自体を主機能にしない。

UI mockupで使った画像サムネイル領域は実運用では不要と判断。
その空間は:
- 辞書一覧
- 日本語説明
- 関連 / 派生
- Prompt確認

に使う。

## 7. Noob / WAI17への考え方

想定利用モデルとして NoobAI / WAI Illustrious 系を意識するが、辞書探索そのものをモデルごとに分断しない方向。

共通コア:
- 日本語で理解
- タグを発見
- 自分で選択
- canonical Englishで出力

モデル別の推奨順序・品質タグ・有効性ヒント等は、必要性が確認できてから別レイヤーで扱う。

v1の基本探索UIをモデル固有知識で複雑化しない。

## 8. UIの見た目に関する暫定原則

- 表側はシンプル、裏側はリッチ
- 常時見える情報を増やしすぎない
- 日本語を主にして英語は追跡可能にする
- 「今何をする画面か」が一目で分かる
- 管理画面・監査ツールのような見た目にしない
- 画像生成ソフトと同時起動しやすい軽量なWindowsデスクトップアプリを維持する

## 9. 現在の #66 / PR #67 の扱い

既存の `feature/issue66-v1-app-shell` / PR #67 は、設計議論前に作った **PoC / experimental implementation** として扱う。

- mainへmerge済みとはみなさない
- このPoCに合わせて製品思想を固定しない
- 新しい設計が固まった後、再利用できる部分だけ評価する
- 必要ならapplication layerは作り直してよい
- ただし辞書・canonical identity・監査成果等の高価値データ資産を不用意に削除しない

## 10. まだ決めていないこと

今後じっくり決める。

1. 辞書画面の最終レイアウト
2. Prompt編集画面の最終レイアウト
3. 長いPromptで最も操作しやすい表示方式
4. ON/OFFをv1必須にするか
5. weight編集をv1に入れるか
6. お気に入り / 履歴の優先度
7. 「関連・派生」を何の根拠で構築するか
8. 日本語表示と英語表示の切替方法
9. Prompt parserがweight / LoRA / 自由文等をどこまで構造化するか
10. 既存コードの KEEP / WRAP / REWRITE / ARCHIVE / DELETE 判定

## 11. 実装再開条件（暫定）

大規模実装を再開する前に、最低限以下を合意する。

- 製品の中心体験
- 辞書画面とPrompt画面の責務
- 主要ユーザーフロー
- 表示と出力の分離
- Prompt貼り付け時の保持ルール
- Special / General / #64の境界
- v1に入れない機能

その後、Astra等に既存コードを棚卸しさせ、
`KEEP / WRAP / REWRITE / ARCHIVE / DELETE`
を決めてから本格実装へ進む。

## 12. 現時点の短縮版

> **DanbooruTagTool は、日本語でDanbooruタグを辞書のように眺め、ジャンルや関連概念から知らないタグを掘り、選んだものを自分のPromptへ追加し、既存Promptも日本語付きで理解・整理して、最後は英語Promptとしてコピーできるローカルツールを目指している。**
>
> **ただし、この具体的なUI / interaction designはまだ設計議論中であり、変更してよい。**
