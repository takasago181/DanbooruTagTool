# Issue #30 最小レビュー用かんたん日本語ガイド

## あなたが最初にすること

19枚全部ではなく、まず1〜5の5枚だけ見てください。PowerShell、GitHub、token操作は不要です。

[最小レビュー用画像一覧を開く](C:/Users/takas/Downloads/StabilityMatrix-win-x64/Data/Models/Issue30Evaluators/issue30_pilot_20260910/review_queue/minimal_review_contact_sheet.png)

画像一覧が開けない場合は、エクスプローラーで次のファイルを開きます。

`C:\Users\takas\Downloads\StabilityMatrix-win-x64\Data\Models\Issue30Evaluators\issue30_pilot_20260910\review_queue\minimal_review_contact_sheet.png`

## 1〜5で見る内容

Taggerの結果やAUTO判定は見ないでください。画像だけを見て、次の3点を答えます。

1. **指定した対象が画像に実際にあるか**
   - ある
   - ない
   - 判断できない
2. **指定していない余計な意味に見えないか**
   - ない
   - ある
   - 判断できない
3. **Stage10で人間の確認を減らす材料として使えそうか**
   - 使える
   - 使えない
   - 判断できない

### 画像ごとの質問

| 番号 | 画像 | 質問 |
| ---: | --- | --- |
| 1 | `anus` | 肛門が画像にはっきり見えるか |
| 2 | `anus peek` | 肛門が隠れず、実際に見えているか |
| 3 | `anus peek` | 2番と同じ質問。別seedの画像で安定しているか |
| 4 | `spread anus` | 指定した「開いた状態」が実際に見えるか |
| 5 | `exposed genitals` 対照画像 | 似た構図でも、指定対象が本当にあるか。対象がなければ「ない」 |

## 返信方法

この5行だけをチャットに返してください。CSV編集は不要です。

```text
1: 対象=ある/ない/判断できない、余計な意味=ない/ある/判断できない、Stage10=使える/使えない/判断できない
2: 対象=ある/ない/判断できない、余計な意味=ない/ある/判断できない、Stage10=使える/使えない/判断できない
3: 対象=ある/ない/判断できない、余計な意味=ない/ある/判断できない、Stage10=使える/使えない/判断できない
4: 対象=ある/ない/判断できない、余計な意味=ない/ある/判断できない、Stage10=使える/使えない/判断できない
5: 対象=ある/ない/判断できない、余計な意味=ない/ある/判断できない、Stage10=使える/使えない/判断できない
```

## 途中で止めてよい条件

1〜4のうち、対象が「ない」なのにTaggerが強く通していそうな画像が2枚あれば、そこで止めてください。残りの画像は見なくて構いません。

「判断できない」が出ても無理に決めず、そのまま返してください。`判断できない` は失敗ではなく、HUMAN REVIEWへ残すための重要な結果です。

この5枚の結果を確認してから、必要な場合だけ6枚目以降を案内します。relation/binding系の画像を今すぐ大量に見る必要はありません。
